There are two schemas for the assistants application:
shared_schema
assist_schema

the shared_schema holds all shared data
the assist_schema holds all multi tenant data


the connection string for the local database is:

localhost:5432
username: postgres
password: postgres
